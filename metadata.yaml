name: mongodb-k8s
summary: A mongodb of an operator framework k8s charm
description: |
  Describe your charm here
series:
  - kubernetes
min-juju-version: 2.7.0
provides:
  mongodb:
    interface: mongodb
peers:
  cluster:
    interface: mongodb-cluster
deployment:
  type: stateful
  service: cluster
storage:
    database:
        type: filesystem
        location: /data/db